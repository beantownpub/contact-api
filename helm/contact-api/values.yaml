appName: contact-api
global:
  env: dev
image:
  repository: jalgraves/contact_api
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

containerPort: 5012

ingress:
  enabled: false

db:
  dbName: contact
  host:
  port:

logLevel: INFO
database:
  enabled: false

regionCode: use2
